name: Publish GitHub Action

on:
  push:
    branches:
      - "main"

jobs:
  release:
    name: Release semver
    uses: vivantehealth/github-actions-workflows/.github/workflows/semver-release.yml@v0
    with:
      default-bump: patch
